{
  "name": "lab-l9",
  "version": "1.0.0",
  "description": "Lab L-9: User Authentication App",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "lars": "lars",
    "lars:config": "lars config",
    "start": "npm --prefix backend run start & AUTHSERVER_PID=$!; npm --prefix frontend run start & FRONTEND_PID=$!; trap 'kill $AUTHSERVER_PID $FRONTEND_PID' INT TERM EXIT; lars start",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@bsv/lars": "^1.5.0",
    "@bsv/sdk": "^1.5.2"
  }
}